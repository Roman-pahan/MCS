/** ЗАДАЧА 6 - Типы данных
 *
 * 1. Объявите несколько переменных и присвойте им значения:
 *  - строка
 *  - число
 *  - логическое
 *  - null
 *  - undefined
 *  - объект
 *  - массив
 *
 * 2. Выведите в консоль тип каждого из значений
 * используя оператор typeof
 */

const myString = "Roman"
const myNumber = 32
const myForce = true
const myHave = null
const myCondition = undefined
const myObject = {name: "Roman", age: 32}
const myArray = ["money","keys", "phone"]

console.log(typeof(myString),typeof(myNumber),typeof(myForce),typeof(myHave),typeof(myCondition),typeof(myObject),typeof(myArray))
